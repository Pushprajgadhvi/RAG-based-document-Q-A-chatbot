# ─────────────────────────────────────────────
# RAG Chatbot - Python Dependencies
# ─────────────────────────────────────────────

# Web Framework
fastapi==0.111.0          # Modern Python API framework (like Express.js for Python)
uvicorn[standard]==0.30.1 # ASGI server to run FastAPI

# Document Parsing
pdfplumber==0.11.0        # PDF text extraction (handles tables, columns)
python-docx==1.1.2        # Word document (.docx) text extraction

# ML / Embeddings
sentence-transformers==3.0.1   # Converts text to vectors using 'all-MiniLM-L6-v2'
faiss-cpu>=1.9.0               # Facebook AI Similarity Search - vector database
numpy==1.26.4                  # Array math for embeddings

# LLM Provider (Groq = free, very fast)
groq==0.9.0               # Groq Python SDK for LLaMA3 access

# Utilities
python-multipart==0.0.9   # Required by FastAPI for file uploads
pydantic==2.7.4           # Data validation for request/response models
python-dotenv==1.0.1      # Load API keys from .env file
